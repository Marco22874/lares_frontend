---
/**
 * @file fr/galerie.astro
 * @author Lares Cohousing Dev Team
 * @date 2026-02-11
 * @description Gallery page (French). Responsive
 *              image grid with lightbox. Images from
 *              Directus gallery collection.
 *
 * @returns Rendered gallery page
 * @update 2026-02-11
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import GalleryGrid from
  '@components/GalleryGrid.astro';

const locale = 'fr';

/*
 * Production: fetch from Directus
 * const images = await getCollection(
 *   'gallery', locale,
 *   { sort: 'sort',
 *     'filter[status][_eq]': 'published' }
 * );
 */
const images = [];
---

<BaseLayout
  title="Galerie"
  description="Explorez les images de Lares Cohousing."
  locale={locale}
  currentPath="gallery"
  pagePath="galerie"
>
  <section class="section">
    <div class="container">
      <h1 class="section__title">Galerie</h1>
      <GalleryGrid
        images={images}
        locale={locale}
      />
    </div>
  </section>
</BaseLayout>
