---
/**
 * @file fr/contact.astro
 * @author Lares Cohousing Dev Team
 * @date 2026-02-11
 * @description Contact page (French). Form with
 *              whitelist validation, honeypot, and
 *              server-side submission to prevent
 *              XSS and SQL injection.
 *
 * @returns Rendered contact page
 * @update 2026-02-11
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import ContactForm from
  '@components/ContactForm.astro';

const locale = 'fr';
const directusUrl =
  import.meta.env.PUBLIC_DIRECTUS_URL || '';
---

<BaseLayout
  title="Contact"
  description="Contactez-nous pour des informations sur Lares Cohousing."
  locale={locale}
  currentPath="contact"
  pagePath="contact"
>
  <section class="section">
    <div
      class="container container--narrow"
      data-directus-url={directusUrl}
    >
      <h1 class="section__title">Contact</h1>
      <ContactForm locale={locale} />
    </div>
  </section>
</BaseLayout>
